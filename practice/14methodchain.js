// method chaining is calling multiple methods in one line

let username = "f8tum";
console.log(username.trim().charAt(0).toUpperCase() + username.trim().slice(1).toLowerCase());
// capitalizes the first letter