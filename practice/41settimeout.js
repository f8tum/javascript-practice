// setTimeout() = function that llows you to schedule
//                the execution of a function after an amount
//                of time (milliseconds), approximate time
//                setTimeout(callback, delay);

function Alert()
{
  window.alert("Hello");
}

setTimeout(Alert, 3000);